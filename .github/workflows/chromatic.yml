# Workflow name
name: "DJ 심청이 CI"

on:
  push:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: yarn
      - uses: chromaui/action@v1
        with:
          projectToken: 10b69a5dab76
          token: ${{ secrets.GITHUB_TOKEN }}
