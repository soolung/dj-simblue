name: Build & Deploy DJ Simblue
on:
  push:
    branches: [dist]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Github remote
        run: git remote set-url --push origin https://kimhyerin0909:${{ secrets.GITHUB_TOKEN }}@github.com/dj-simblue
      - name: Install and Build 🔧
        run: |
          yarn install
          yarn deploy-storybook -- --ci
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
